// Route wrapper — actual component lives in src/screens/LoginScreen.tsx
// so it can be imported normally from _layout.tsx without Expo Router conflicts.
export { default } from '../src/screens/LoginScreen';
